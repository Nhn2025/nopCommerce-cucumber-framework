name: Run Tests & Publish Allure Report

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: maven

      - name: Install Allure
        run: |
          wget https://github.com/allure-framework/allure2/releases/download/2.24.0/allure-2.24.0.tgz
          tar -zxvf allure-2.24.0.tgz
          sudo mv allure-2.24.0 /opt/allure
          sudo ln -s /opt/allure/bin/allure /usr/bin/allure

      - name: Run tests
        run: mvn clean test
        continue-on-error: true   # 🔥 QUAN TRỌNG

      - name: List target folder (debug)
        if: always()
        run: ls -R target || true

      - name: Generate Allure report
        if: always()              # 🔥 QUAN TRỌNG
        run: allure generate target/allure-results --clean -o target/allure-report || true

      - name: Upload report as artifact
        if: always()              # 🔥 QUAN TRỌNG
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: target/allure-report